import React from 'react';
import styled from '@emotion/styled';

const Heading = styled.h1`
    color: #001452;
`;

const Header = () => (
    <Heading>Location search</Heading>
);

export default Header;